# Upstream template workflows

These workflows are retained for reference from al-folio. They are inactive for this personal website because they maintain the upstream template, test its demo content, or generate content this site does not use.

Active website deployment and formatting checks live in `../workflows/`. The CV PDF is compiled from a separately maintained LaTeX source.
